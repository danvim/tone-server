import { Player } from "./Player";
import { Protocol } from "tone-core";
export class Lobby {
  players: Array<Player>;
  // protocol: Protocol;
  constructor() {
    this.players = [];
  }
  // initProtocol(protocol:Protocol){
  //   // protocol.on(PackageType.)
  // }
}
